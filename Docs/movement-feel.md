# Movement Feel — Character Controller

**MUST READ before touching walk/sprint speed, turn feel, camera weight, starts/stops/pivots, the moving-turn layer, or the locomotion blendspaces.** Split out of CLAUDE.md on 2026-08-16 — text unchanged. Italic `see *Section*` pointers may refer to a sibling doc; the Documentation Map in `CLAUDE.md` says which one owns it.

## Character Controller — current feel & tuning (reference)

The bullets below document the **current state** of the movement/camera/turn/jump feel — the values that shipped from each iteration pass and the reasoning behind them. Nothing here is locked; the controller is open to iteration (as of 2026-07-04 an AnimX-kit-informed pass is planned: quadruped IK port, predictive landing, additive layers — see the migration doc in the AnimXLowPolyCats project). The value of this section is *deliberateness*: know what the current numbers are and why before changing them, so feel changes are intentional rather than side effects. Known open bugs in the jump **landing animation**: the `Land_stop`→Idle snap and front-paw penetration (see *Jump State Machine → OPEN*).

- **Camera-relative movement**: `Move()` projects input onto the camera's yaw plane; the character orients toward movement via `bOrientRotationToMovement`. (Tank controls existed in an earlier iteration and were deliberately replaced — do not reintroduce them.)
- **Walk speed 400 cm/s** (`MovementMaxWalkSpeed` override on PrimeCatBase; the C++ default is 600 but the intended feel is 400 — the constructor-baking bug meant playtesting always happened at 400, so 400 was kept deliberately on 2026-06-09). Acceleration/friction/braking ship at C++ defaults.
- **Gaits — M1 of the weighty-movement pass (2026-07-08, Sean-approved).** **W = trot** (the 400 above), **W+Shift = sprint** (`IA_Sprint`: LeftShift + gamepad stick-click): `bIsSprinting` (replicated, server-authoritative, owner-predicted like the grab drag pattern, listen-host bypass) drives `ApplyGaitMovementSettings` — `SprintMaxWalkSpeed 650` and `SprintRotationRateYaw 150` (vs 200 at trot; wider arcs at speed are the weight differentiation). `SpeedType` now classifies on **absolute** speed (Run ≥ 0.8×sprint = 520, Trot ≥ 0.6×trot = 240 — normalizing by the gait-dependent `MaxWalkSpeed` would call a full trot a Run; Run is only reachable while sprinting). `BS1_Cat_Locomotion`: Idle@0 / Walk@150 / **Trot@400 ×1.7** / Run@650 ×1.4, axis 0..650 (the trot clip had never been in the blendspace — the old Walk@150/Run@600 layout meant 400 played a 60/40 blend; rate scales compensate for clips authored at ~55–60 cm/s stance speed). Grab-drag still owns the CMC during grabs; release restores the *current* gait. Trot speed possibly dropping to ~350 later (Sean's call, needs blendspace sample + rate re-derive alongside `MovementMaxWalkSpeed`).
- **Turn feel — updated 2026-06-19**: `MovementRotationRateYaw 200`°/s on PrimeCatBase (down from the 360 C++ default) for a weightier, *arcing* turn instead of an instant snap toward the movement vector. The locomotion **lean** banks the spine from the **signed angle between velocity and input acceleration** (`UpdateCosmeticInterpolation` section D — not the old yaw-rate signal), so the bank *holds through the turn* while the body catches up; wired to `Spine_2` **Pitch ×18** in ABP_Cat_V2 (accel/decel lean is on `Spine_2` Roll). This is still an **orient-to-movement** model (the cat faces its motion). A true aim-facing **strafe** rework (2D blendspace) is a known future option — NOT done; flagged for a later refactor.
- **Turn-in-place — procedural, added 2026-06-19**: when the cat is **idle** and the camera yaw diverges from the body past `TurnInPlaceThreshold` (35° on PrimeCatBase; 50 C++ default), `ACatBase::UpdateTurnInPlace` rotates the body toward the camera by the **shortest signed angle** at a capped rate (constexpr `TurnSpeedDegPerSec` 150, eases out via `FMath::FixedTurn`), with hysteresis (disengages within ~4°, so it can't wrap or chatter). It drives the **in-place** `BS1_Cat_Turn` blendspace (in the `Locomotion_v2` Turn state) via a signed `TurnRateAnim` (−1..+1) computed from the applied yaw rate, and sets `SpeedType=Turn` + `bGoTurn` (the latter replicated `COND_SkipOwner` so proxies enter the Turn state and read the replicated `TurnRateAnim`; the body rotation itself replicates through the CMC). The four turn clips (`A_Cat_Move_Turn-45/90_L/R`) have **`force_root_lock=True`** so they render as pure in-place footwork — without it, with the AnimBP on `RootMotionFromMontagesOnly` the clips' baked rotation plays in the pose *on top of* the procedural rotation and pops each loop. The AnimGraph's `DefaultSlot` node was also wired into the pose chain (it had been orphaned). This **replaces** a shelved root-motion-turn-**montage** attempt — discrete 45/90 montages couldn't keep pace with camera input (lagged seconds behind on fast turns, wrapped at ±180); the procedural model tracks continuously. Turn-speed/disengage/interp are constexpr in `UpdateTurnInPlace`; `TurnInPlaceThreshold` is the only exposed UPROPERTY.
- **Jump tuning — current values (platforming rework, 2026-06-18)**: `JumpLaunchVelocity 700`, `GravityScaleRising 2.0`, `GravityScaleApex 3.4`, `GravityScaleFalling 5.5`, `GravityScaleInterpSpeed 25`, `ApexVelocityThreshold 30`, `JumpAirControl 0.7`, `JumpMaxHoldTimeTuning 0.18`, `CoyoteTime 0.12`, `JumpBufferTime 0.15`, `JumpCooldown 0.05`, `LandRecoveryDuration 0.25`, `MinFallTransitionHoldTime 0.05` (reduced from 0.30 on 2026-06-20 — the 0.30 hold kept the cat in the apex pose through a fast descent and snapped the fall blendspace; this only gates the *anim* phase, not the physics arc), `HardLandSpeedThreshold 900`; added 2026-07-05 (header-only defaults, cosmetic — no PrimeCatBase mirrors): `LandPredictTimeMoving 0.08`, `LandPredictTimeStopping 0.12` (predictive-landing lookahead), `LandCushionMaxDip 10`, `LandCushionDipPerImpact 0.01`, `LandCushionFrequency 14`, `LandCushionDampingRatio 0.85` (+ `bEnableLandCushion`, see *Jump State Machine*). Yields apex ~125 cm in ~0.36 s rise. These are baked into the C++ header defaults **and** mirrored as redundant PrimeCatBase overrides (equal values) — if you change one, change both so they stay in sync. (`MinFallTransitionHoldTime` is now header-only — the redundant PrimeCatBase override was cleared 2026-06-20; setting an inherited C++ UPROPERTY default via the CDO does **not** propagate to spawned pawns, so change it in the header.) Coyote time + jump buffer are shipped (see *Jump State Machine*).

- **Camera weight — M3 of the weighty-movement pass (2026-07-14, Sean-approved).** `ACatBase::UpdateCameraWeight` — locally controlled cat only, purely cosmetic, zero replication surface; **header-only defaults** (no PrimeCatBase mirrors), all live-tunable under `Camera|Weight`, master switch `bEnableCameraWeight`. Four pieces: (1) **per-gait spring-arm lag** — `CameraLagSpeed/CameraRotationLagSpeed` (10/8) lerp toward `SprintCameraLagSpeed 6` / `SprintCameraRotationLagSpeed 7` by **actual speed** over the trot→sprint band (400→650), so partial speeds land in between; the blend **rises with raw speed but relaxes back through `CameraGaitRelaxSpeed 2.0`** — an instantaneous release snapped the lag to base mid-brake and the camera lurched onto the stopped cat (2026-07-14 fix, the one iteration of the pass). Note the trailing direction is **opposite the AnimX kit's** (kit goes snappier at run); it read fine in PIE, but if it ever reads wrong, flip the Sprint* values above base rather than re-derive. (2) **Sprint FOV push** `SprintFOVPush 3`° (first pass shipped 6 — too much), `CameraFOVInterpSpeed 5`, target rides the relaxed blend so the zoom-out eases with the lag. (3) **Landing dip** — damped spring on the boom's `SocketOffset.Z` (same clamped-step ≤1/30 semi-implicit form as the landing cushion): kick = `LandImpactIntensity × CameraLandDipMax 8` cm, `CameraDipFrequency 9`, `CameraDipDampingRatio 0.9`, clamp −Max..+Max/4. (4) **Stop-settle** — halting kicks `StopSettleDip 4` cm scaled by **peak speed since the last stop** mapped over the same trot→sprint band (a plain trot stop is silent); kicks are **additive into the same spring** so a landing and a settle compose instead of clobbering. `BeginPlay` captures the authored `SocketOffset.Z`/FOV as the rest values (`CamBaseSocketOffsetZ`/`CameraBaseFOV`) — the spring and push offset from those, and the kill-switch path restores them.

- **Moving-turn layer — M2 (part 1) of the weighty-movement pass (2026-07-14, Sean-approved).** The `Move` state now plays **`BS2_Cat_Locomotion`** (`/Game/Blueprints`, 2D: X = Turn −1..+1 grid 4, Y = Speed 0..650 grid 13, weight-interp 5.0) instead of the 1D BS1 — **X is fed by `LeanAmount`** (the existing signed steering-angle cosmetic, ±1 at 60°; same signal as the spine bank, so the authored lean and the procedural bank stack — reads right in PIE), Y by `Speed`. 10 samples: `_F` cycles at X=0 and the kit's **L/R lean cycles at X=±1** per gait row (Walk@150, Trot@400 ×1.7, Run@650 ×1.4 — M1 rates). **Two traps burned into this build:** (1) the kit-faithful first layout embedded the Turn-45/90 *footwork* clips as columns at X≈±0.8/±1 (the kit FC model) — our steering signal saturates at a mouse flick, so gait↔footwork blending warped the normalized-time sync into constant hitching; the columns were removed (idle footwork already lives in the separate Turn state; a *moving* pivot is the dedicated M2 layer still to come). (2) The lean cycles (and `Trot_F`) shipped `force_root_lock=False` — their baked root arcs played in-pose under `RootMotionFromMontagesOnly` and popped every loop (the same June lesson as the turn-in-place clips; the per-loop pop was the residual hitch after fix 1). All nine gait clips are now root-locked. `BS1_Cat_Locomotion` is untouched as rollback. The sharp-sustained-steer edge case this bullet used to flag is now handled by the **moving pivot** (next bullet).

- **Moving pivot — M2 part 2 (plant-and-turn), 2026-07-18 (commit `0380e25`, Sean-gated with a documented interim flaw).** `ACatBase::UpdateMovingPivot` (locally controlled only, runs before `UpdateTurnInPlace` in the tick): a hard sustained steer at speed plants the cat and turns it with footwork instead of the orient-to-movement swing arc. While pivoting: `Move()` suppresses its `AddMovementInput` (zero accel → friction brakes + orient-to-movement inert), braking boosted to `PivotBrakingDeceleration 2000` (client-predicted + mirrored via `Server_SetPivotBraking`, the grab-prediction pattern; restored to `MovementBrakingDeceleration` on exit), body rotates toward the **live input direction** (cached by `Move()` each frame) at `PivotTurnSpeedDegPerSec 200` via `FixedTurn`, `SpeedType=Turn` + `TurnRateAnim` drive the `BS1_Cat_Turn` footwork through the **shared turn-in-place interp/RPC block** (fast attack interp 30 while pivoting; MP rides the existing turn RPCs/bGoTurn), and the landing-cushion spring takes a speed-scaled plant kick (camera stop-settle fires by itself as speed collapses). Exit at `PivotExitAngle 15°` from the input direction, or on input release/airborne/grab; `PivotCooldown 0.3`. **Two-tier trigger (all header-only knobs, derived from three PIE diag rounds — velocity CHASES the input at the CMC rotation rate, so a naive large-angle threshold never fires):** flip tier ≥ `PivotFlipAngle 110°` (hard 180/S-flick, input constant); sweep tier ≥ `PivotAngleThreshold 55°` **plus ≥ `PivotSweepMinDeg 15°` of accumulated input-direction rotation** over the `PivotSustainTime 0.12 s` window (discriminates camera sweeps from ordinary 90° direction taps, which cross the same angle band with a constant input); arm speed `PivotMinSpeed 240` is checked **only on the window's first frame** (the steer itself brakes the cat — continuous checking voided the trigger right as the angle peaked; a near-stop <100 still cancels). ABP: `Move → Turn` transition added (`SpeedType == Turn`, 0.12 blend); the pre-existing `Turn → Move` rule (`!= Idle && != Turn`) handles the exit. Residual known edge: a *slow* sweep whose rate matches the velocity chase equilibrates at 30–45° — under the trigger, reads as a normal arc (deliberately untriggered; tune with eyes if it ever bothers). **RESOLVED — `PivotFootworkCap` (was 0.4, DELETED 2026-08-09; the real fix, the authored Pivot90/180 clips, shipped 2026-07-22 — see the next bullet):** the `A_Cat_Move_Turn-45/90` clips are the kit's *moving*-turn clips with stride-elevated footwork through their whole range — the 2026-07-18 paw sampler measured all four paws hovering 3–7 cm (never reaching the −22.5 contact plane) at ANY sustained blend position, so no cap value reads planted (lower slides, higher paddles; this also affects sustained saturated turn-in-place, it just historically never held saturation long enough to notice). The real fix is a dedicated CtrlRig-authored plant-pivot clip — **M5 asset batch** (with the run-jump-start and clamber clips); the pivot mechanics stay as-is and the clip drops in.

- **M5 pivot-clip round 1 — draft clips REVERTED, scrub infrastructure kept dormant (2026-07-20).** The cap flaw was attacked with procedurally keyed draft clips (`/Game/Drafts/M5/A_Cat_Move_Pivot_L/R_Draft` — AnimPose-sampled kit keyposes lerped into new sequences via `seq.controller`; plant → crouch-turn steps → gathered coil, root-locked, 0.6 s) and a full scrub pipeline that is now built, MP-ready, and parked: C++ `bGoPivot` (replicated `COND_SkipOwner`, owner-predicted, rides the `Server_SetPivotBraking` enter/exit edges) + `PivotProgress` (0→1 over the **reachable** angle = entry−`PivotExitAngle`, monotonic against target drift, streamed via `Server_SetPivotProgress` on 0.05 deltas, snapped to 1 on exit) → ABP Turn state Blend-by-bool → per-hand Sequence **Evaluators** (`ExplicitTime = PivotProgress × 0.6`, L when `TurnRateAnim < 0`). **Two Sean-gated iterations produced the real M5 authoring spec:** (1) **the clip must END on the gathered coil — no push-off extension**: at exit the cat is at Speed 0 and re-accelerates through Move, so a baked extension reads as an uncaused lunge (v1's `Jump_InPlace@0.45` keypose also turned out to be the vertical-jump REAR-UP, not a forward load); the launch read comes free from the start-burst accel-lean rail + gait blend-in (the `StopPlantDip 0` lesson again); also map the scrub over the reachable angle or the clip tail snaps on the exit frame. (2) **Lerped keyposes cannot render mid-pivot footwork**: measured pivots span 0.32 s (S-flick, 5-pose arc = stutter) to 0.88 s (sprint-180, two static step samples = footfall-less gliding) — the middle needs real footfall animation and one clip can't serve both durations → author **~90° and 180° CtrlRig variants** (select by `PivotInitialAngleDeg`), paws to the −22.5 contact plane during stance. **Live config = BS1_Cat_Turn capped footwork again** (the outer blend's `bActiveValue` is disconnected, default False). To re-arm when the real clips land: reconnect `Get bGoPivot → bActiveValue` in the Turn state and drop the clips into the two evaluators. **Same-day follow-up — turn stance drop (Sean-approved via live tune):** re-measuring the turn clips with a trustworthy eval overturned part of the July hover diagnosis — hind + inside-fore paws DO plant in the raw clips (minima 2.0–2.8 ≈ trot stance); only the **outside forepaw** never reaches contact (min 3.5–4.3, the kit's moving-turn reach arc). Fix = `TurnStanceDrop 3.5` + `TurnStanceDropEngageSpeed 22` (`Movement Tuning|Turn`, header-only): cosmetic mesh drop while `SpeedType == Turn`, composed into the cushion/conform transform write (§B2); engage must beat the Turn-state blend-in (the 1.8-at-speed-8 first pass left the opening steps airborne — telemetry-confirmed, then live-tuned 1.8→3.5 during PIE). Accepted residuals: back paws clip the ground slightly, faint rise on close inspection; candidate polish = re-enable the (upward-only) foot IK during Turn to lift the clippers — it was gated off in the July MP pass, reason unrecorded, so probe before trusting. PawPrint gained `bTurnInPlace` + `TurnDropZ` channels.

- **M5 pivot round 2 — Blender-authored Pivot90/180 clips SHIPPED, scrub pipeline LIVE (2026-07-22, Sean-gated over three PIE rounds).** `/Game/Drafts/M5/A_Cat_Move_Pivot90_L/R` (1.0 s) + `Pivot180_L/R` (2.0 s, the 90 footwork concatenated twice = 4 real footfall beats) — built scripted in `Saved/BlenderExchange/M5_authoring.blend` (reproducible; recipe + traps in the `blender-roundtrip-recipe` memory): kit Turn-90 footwork **root-locked** (paw counter-rotation lives in the leg-chain curves, so real footfalls survive; the baked yaw is linear-in-time ±1° = matches the linear scrub), coil ending blended from `CrouchIdle` f1 (NOT `Jump_InPlace` — that crouch is the vertical rear-up), outside-forepaw swing retargeted to land gathered (the kit's 16 cm reach-arc), temp-IK bake against per-frame ground targets (0.003 cm). **THE DISCOVERY THAT SHAPES ALL FUTURE CLIP AUTHORING: `CatMnq_Skeleton` runs SKELETON translation retargeting on ALL 54 bones — every clip evaluates rotation-only; authored hip/body translation is DISCARDED** (why the kit clips are conveniently "in-place", why the July draft crouch never rendered, and why the landing cushion/stance-drop systems exist). Do NOT flip the skeleton (live clips carry discarded translation everywhere — trot bob, jump spine 12–20 cm — and the tuned systems assume it stays dead); author body-height motion as a **procedural mesh drop synced to the scrub**: `PivotCrouchDrop[11]` analytic table in §B2 (drop = 0.26 + 8.84·envelope(progress); the clips' spine-translation base is flattened in Blender so ONE table is exact for both lengths), ×`PivotCrouchScale` (header knob), supersedes `TurnStanceDrop` while `bGoPivot`, tracked at speed 60 while pivoting (engage-22 lagged the fast scrub 1–1.5 cm). **Scrub rework (round-2 fix, PawPrint-diagnosed):** `PivotProgress` = accumulated APPLIED rotation over a drift-growing denominator (`PivotAccumDeg`; denom = max(latched reachable, accum+remaining−exit) → exactly 1 at exit) — the old remaining-vs-live-target form FROZE mid-clip during camera sweeps (target drifts as fast as the body chases; held beat-1's swing paw mid-air, then played the back 80% at 3×). Residual: a camera outrunning the body briefly holds progress (monotonic clamp) ~0.15–0.4 s — benign, watch on slow deliberate sweeps. **Cadence (round-3 fix): `PivotTurnSpeedDegPerSec` IS the footfall-speed knob** — playback ≈ rate ÷ 82 (the clips' authored turning rate); 200 = 2.4× tippy-tap paws; ships **120** (~1.45×), ClampMin lowered to 60 so the authored-1× zone (~80: 90° in ~0.95 s, 180° in ~2 s) is live-tunable. Note turn-in-place rotates at constexpr 150 — reconcile if Sean lands far below. ABP: Turn state selects the 90 vs 180 pair on **`PivotAngleDeg` ≥ 110** (new UPROPERTY, latched at entry so a growing sweep can't switch clips mid-pivot, rides `Server_SetPivotBraking`, `COND_SkipOwner`; EventGraph copy + per-pair ExplicitTime multipliers 1.0/2.0); `bGoPivot → bActiveValue` re-armed. `PivotFootworkCap` was **DELETED 2026-08-09 (bug bash BB-09/BB-23)** — the claim that it "only shapes the BS1 fallback (non-pivot turns)" was wrong twice over: non-pivot turns are turn-in-place, and `UpdateTurnInPlace` never multiplied by the cap. It scaled a `TurnRateAnim` magnitude that, during a pivot, the ABP read *only* as an L/R sign test; latching the side into `bPivotLeft` (BB-08) removed even that. Turn-in-place footwork is handled by `TurnStanceDrop`. v1 residuals: R-clip heading drifts ~8° (unmirrored coil body pose — mirror if it reads); stop→standstill→camera-flip correctly plays turn-in-place re-face, NOT the pivot (arm speed 240 gate).
- **Skid-stop clip — SHIPPED 2026-07-22 (dedicated Skid state + scrub, Sean-approved; telemetry-verified 0.001 cm drop tracking).** `A_Cat_Stop_Skid` (0.8 s, `/Game/Drafts/M5`) plays for **sprint-band stops only** (`SkidMinEntrySpeed 500`; trot stops keep the plain gait run-out by design). Keypose-built in `M5_authoring.blend` (legit for skids — no footfalls): entry = `Run_F` f13, brace = `Jump_Land_run` f05 (the four-paw momentum absorb), settle = `Idle_Base` f1, paws IK-pinned throughout. C++: `bGoSkid` + `SkidProgress` (COND_SkipOwner; enter rides `Server_SetStopBraking(bApply,bSkid)`, progress streams via `Server_SetSkidProgress`); scrub = (1−v/entry)/(1−plant/entry) — linear in time under the constant-decel stop, exactly 1 at the plant; **snap-1 on plant, FREEZE on abort** (blend-out leaves from the actual pose). Brace crouch = `SkidCrouchDrop[11]` (peak 7.8 = the Land_run source depth; 6.0 left the pitched pelvis's hind sockets beyond leg reach) × `SkidCrouchScale` in §B2, precedence pivot > skid > turn. ABP: `Skid` state (evaluator × 0.8) with Move→Skid (`bGoSkid`, 0.12), Skid→Move (`!bGoSkid && Speed>150`, 0.2), Skid→Idle (`!bGoSkid && Speed≤150`, 0.3); post-plant re-face rides Idle→Turn (settle-then-turn). **Two rules burned in (apply to ALL future authored clips):** (1) **zero EVERY non-Spine bone translation before the IK bake** — the kit animates Shoulder translation ~1.7 cm (scapular slide) + Thigh ~2 cm, UE's all-SKELETON retargeting discards them, and an IK solution that leaned on them renders with fore legs floating ~1.5 cm (sustained precision poses expose what moving clips mask; the four pivot clips still carry kit-baseline shoulder translation — unflagged, polish candidate); (2) **§B2 scrub-driven drops track their table EXACTLY while sustained** (snap, not interp — the tables start ≈0 so the enter edge needs no smoothing; interp covers enter/exit only) — the speed-60 interp still lagged the skid's fast rise 0.3–0.95 cm, floating all paws at the plant moment. Known composition (by design, verified in data): the railed decel-lean additive noses the front down ~2–3 cm through the settle and the upward-only foot IK (alpha ramps in as speed falls) keeps the paws planted — the landing-cushion pattern. Entry keeps ~0.1 s of blend-window penetration at full sprint (IK correctly off at that speed) — accepted. Skid ABP-graph recipes (transition rule graphs all share the name 'Transition' — rename bound graphs at UObject level, verify mapping by crossfade_duration) live in the memory notes.
- **Start-step clip — SHIPPED 2026-07-22 (Start state + coil/burst scrub; telemetry: every launch exactly on the July spec — burst END 406 cm/s, 68 cm, 0.33 s; 0.001 cm drop tracking).** `A_Cat_Start_Step` (0.5 s): idle → loaded coil (50% blend toward `Jump_Run` f4 — the RUNNING-leap gather, not the vertical rear-up) at `StartClipCoilFrac` 0.24 → push-off extension (`Jump_Run` f7: hinds planted driving, fores lifting into the reach) → `Run_F` stride. **Authoring model:** planted paws HOLD their coil component-space position through the push — the capsule's real acceleration renders the push-off; the crouch envelope HOLDS through the push (the extension pose needs the low pelvis), releasing at the stride handoff (`StartCrouchDrop` table peak 4.5 in §B2 × `StartCrouchScale`, which when > 0 REPLACES the springy `StartCoilDip` cushion kick — double-dip guard; zeroing the scale restores the kick). C++: `bGoStartStep`/`StartProgress` (COND_SkipOwner; `Server_SetStartStep`/`Server_SetStartProgress`), scrub = coil timer → coil frac, then burst maps the remainder by Speed/`StartBurstEndSpeed` (the launch pose tracks the physical acceleration), snap-1 at burst end / freeze on abort; burst-only mode (StartCoilTime 0) still arms the clip. ABP: `Start` state (evaluator ×0.5) entered from Idle OR Move on `bGoStartStep` (the trigger fires ≤100 speed — the SM can be in either), exits Move (>150) / Idle (≤150) on disarm. Graph-surgery trap #2: mapping new transition rule graphs by crossfade duration COLLIDES with pre-existing transitions at common durations — identify by outer `AnimStateTransitionNode_N` creation suffix FIRST, then map durations within that set. **All six M5 clips are STUBS by design** (procedurally assembled kit keyposes, `/Game/Drafts/M5`): the durable layer is the scrub contracts + ABP states + C++ + `M5_authoring.blend`; polish/replacement is a pure content swap respecting three invariants — non-Spine translations zero, hip follows the drop envelope (analytic, re-derivable), clip length matches the ABP multiplier.
- **Weighty stops — M2 part 2b, 2026-07-19 (commit `2915bd9`, Sean-approved: "reading good").** `ACatBase::UpdateWeightyStop` (local owner, runs after `UpdateMovingPivot` in the tick — a pivot supersedes, and its input suppression must not read as a release): releasing input at ≥ `StopMinSpeed 300` while grounded (had-input→released **edge**, so external shoves can't trigger it) swaps CMC braking to `bUseSeparateBrakingFriction` + `StopBrakingFriction 0` + constant `StopBrakingDeceleration 1000` (client-predicted + `Server_SetStopBraking` mirror, the pivot RPC pattern; restore defers to a live pivot). Constant decel makes stop distance v²/2a — **sprint ≈ 209 cm/0.57 s vs trot ≈ 77 cm/0.32 s from one knob** (the old friction-dominated braking halted a sprint in ~52 cm, barely gait-differentiated: exponential decay compresses entry-speed differences). Plant at ≤ `StopPlantSpeed 80` arms an entry-speed-scaled **re-accel ramp** (`StopReaccelDelay 0.18`; `Move()` scales input 0→1 over the window); aborts on input return/airborne/grab/pivot/1.2 s failsafe. All header-only knobs under `Movement Tuning|Stop`, master `bEnableWeightyStops`; no ABP changes — the cat strides down through the gait blendspace (run-out stop; a skid clip is on the **M5 batch**, and each stop logs entry speed + run-out distance/duration = the authoring spec). **Tuning history burned in:** first pass `1400`/`StopPlantDip 6` — the sprint glide didn't register on screen (translation alone isn't weight) and trot stops are *imperceptibly* different by design (differentiation lives at sprint); v2 `1000`/dip 10 made the glide read but the plant kick was a "funky spine pop" — a live isolation probe (dip→0 during PIE) confirmed the kick as the offender, and **`StopPlantDip` ships 0**: the accel-lean spring's release overshoot (target un-railing from −1 at the halt) already gives the stop its rear-up settle for free. Known boundary: releasing Shift *before* W is a gait downshift then a trot stop — if that turns out to be the common stop pattern, a short grace window (W-release within ~0.3 s of Shift-release counts as sprint entry) is the sketched fix, deliberately not built. Landing at speed with no held input also doesn't trigger a run-out (release-edge only) — noted, unbuilt.

- **Weighty starts — M2 part 2c, 2026-07-19 (commit `4ddbf0e`, Sean-approved: coil reads as weight, not lag).** `ACatBase::UpdateWeightyStart` (local owner, runs after `UpdateWeightyStop`): a fresh sprint start from near-standstill (≤ `StartCoilMaxSpeed 100`) **coils** for `StartCoilTime 0.12` (`Move()` suppresses input — the standstill-jump anticipation doctrine; **trot starts stay instant** for platforming) with a `StartCoilDip 4` cushion load, then **bursts** — `MaxAcceleration ×StartBurstAccelMultiplier 2.5` (predicted + `Server_SetStartBurstAccel` mirror) until `StartBurstEndSpeed 400`, then normal accel earns the sprint top end (PIE-measured launch: 0→400 in 0.33 s / 67 cm — the M5 start-clip spec, logged per launch). The lunge pose is the accel-lean spring railing forward through the burst (the stop's free-pose trick, mirrored). **Triggers are EDGE-based** (fresh-input edge while sprinting, OR sprint-engage edge while input fresh — covers both key orders) — never level: sprinting into a wall collapses speed under the coil gate with input held, and a level trigger would coil against the wall. Aborts on release/airborne/grab/pivot; jump press mid-coil hands off to the jump anticipation (`JumpAnticipationTimer` gates entry). **M4 input shaping folded in:** plain fresh input from idle ramps 0→1 over `StartInputRampTime 0.12` in `Move()` (min-composed with the stop's re-accel ramp; skipped on the coil path — the burst is its own envelope). All header-only knobs under `Movement Tuning|Start`, master `bEnableWeightyStarts`; no ABP changes. Boundary (deliberate): sprint engaged **while already moving** gets no launch-step — a stride-synced moving launch is M5-asset territory. **RESOLVED 2026-07-21 — the sprint-pinning wall jerk (head/shoulders)**: PawPrint-diagnosed, NOT an oscillation — pinned at a wall the front paws poke through the face (body longer than capsule) and the paw down-trace STARTS inside the wall's collision → UE returns a start-penetrating hit at the trace start → offset railed to +`FootIKTraceUpDistance` (25) and the penetration snap applied it in ONE frame (measured −0.1→+25.0 cm/frame at wall contact; `ChestDropZ` pumped to ~12 behind it). Fix = trace-validity filter in `SolveFoot`: reject `bStartPenetrating` hits and `ImpactNormal.Z < 0.5` (steeper than ~60° isn't floor) — treated as a miss, so the paw rides FK and the chest/incline aggregation sees no floor. The slope-pitch probes were EXONERATED (the ±25° discontinuity rejection already catches the wall-penetrated probe, which computes exactly 26.6°). Verified in telemetry + Sean PIE pass (wall pins flat at ±0.2 cm; landing snap untouched). Ramp conform not re-verified in data that round (filter can't touch ≤60° slopes by construction — but it's the first suspect if ramps ever regress). 11 `IK_*` PawPrint channels added in `UpdateFootIK` (offsets ×4, front floor-hit flags, ChestDropZ, SlopePitch, InclineF, Alpha, HandL rot-pitch) — permanent.

- **M2 (start/stop/pivot transitions) is COMPLETE (2026-07-19).** Part 2d — the WASD-release re-face footwork gap — was found already closed by the June fix (`65d7217`: turn-in-place engagement gated on `Speed < 10` so the SM settles `Move→Idle→Turn` and the footwork plays; the 07-06 handoff plan carried a stale "still open" line). Sean re-verified the composed behavior with the new stops (run-out → plant → rear-up → re-face) and approved as-is. Note for future turn work: the June gate's rationale ("no `Move→Turn` ABP edge") is obsolete — the pivot added that edge (2026-07-18), so a mid-slide re-face is now *possible* if the two-beat "settle, then turn" ever reads robotic; deliberately not built. The 2-player listen-server sanity pass of the stop/start braking+accel mirrors ran clean same day (Sean-verified; log shows healthy interrupt chains, no net warnings). Remaining initiative work: the M5 clip batch (plant-pivot, skid-stop, start-step, run-jump-start, clamber — authoring specs logged per stop/start in `LogCatVentures`), now **Blender-authored**: the UE↔Blender null round-trip gate PASSED 2026-07-21 (worst drift 0.13°/0.0001 cm on SK_Cat_L + Turn-90_L; recipe + traps — scene-range clamp, deferred python reimport — in the `blender-roundtrip-recipe` memory). The foot-IK wall-pin jerk is FIXED (2026-07-21, see the weighty-starts bullet).

- **Additive idle-life layer — AnimX pass Step 3, 2026-07-06 (awaiting Sean's PIE feel check).** `ABP_Cat_V2`'s post-locomotion chain is now kit-ordered: spine lean → **ears/blink** (Apply Mesh Space Additive ← `AddEarsBlink` SM) → **breath** (Apply Additive, unchanged from June) → **aim** (Apply Mesh Space Additive ← `BS1_Cat_Aim` Blendspace *Evaluator*) → foot IK → out. Ears/blink SM: empty `Neutral` state (an empty state result is the additive identity — no contribution) → `Eyes_Blink` (`A_Cat_Add_Blink`) on `bPlayBlink` / `Ears_Twitch` (Blend-by-Int over `A_Cat_Add_Ears_1/2/3` by `IndexEars`) on `bPlayEarsTwitch`; 0.2 blends, automatic return at clip end, all four players **loop=False** (one-shots). C++ (`UpdateCosmeticInterpolation` B/B2): blink every 3–7 s, twitch every 6–12 s with no back-to-back clip repeat (kit CharBP config values; constexpr, seeded per-machine so cats don't sync), pulse 0.2 s. Aim: the kit bakes the **yaw sweep into the aim clips' timeline** — `AimBSYawTime` (yaw ±180°→0..1, 0.5 centered) scrubs the evaluator's NormalizedTime while `AimBSPitch` (±60°→−1..+1) is the blendspace axis; weight = existing `AlphaAimInterp` (fades 1→0 over speed 0→800). Head-look only tracks on the locally controlled cat (proxies have no ControlRotation → neutral head). Spine-incline additives (`A_Cat_Add_Incline`/`P_Squat`/`P_UpTail`) shipped with the Step 3b spine/incline block (see *Foot IK → Milestone 1b + Step 3b*, 2026-07-06).

All movement tuning UPROPERTYs are re-applied to the CMC in `ACatBase::BeginPlay` (the constructor bakes C++ defaults into the CMC *before* Blueprint serialization, so BeginPlay re-application is what makes PrimeCatBase overrides work — keep new tuning knobs on that list).

